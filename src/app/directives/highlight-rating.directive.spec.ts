import { HighlightRatingDirective } from './highlight-rating.directive';

describe('HighlightRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRatingDirective();
    expect(directive).toBeTruthy();
  });
});
